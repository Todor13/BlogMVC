<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <div class="navbar-default">
            <ul class="nav navbar-nav navbar-left">
                <li>@Html.ActionLink("Home", "Index")</li>
                <li>@Html.ActionLink("Create Thread", "Create")</li>
                <li class="dropdown">@Html.DropDownList("Sections", "Go to Section")</li>
            </ul>
            @using (Html.BeginForm("Search", "Forum", FormMethod.Get, new { @class = "navbar-form navbar-right" }))
            {
                <div class="form-group">
                    @Html.TextBox("query", null, new { @class = "form-control", placeholder = "Search" })
                </div>
                    <input type="submit" class="btn btn-default" value="Search" />
            }
        </div>
    </div>
</nav>