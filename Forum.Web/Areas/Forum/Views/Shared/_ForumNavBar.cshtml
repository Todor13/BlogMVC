<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <div class="navbar-default">
            <ul class="nav navbar-nav navbar-left">
                <li>@Html.ActionLink("Home", "Index", "Home", new { Area = "Forum" }, null)</li>
                @if (Request.IsAuthenticated)
                {
                    <li>@Html.ActionLink("Create Thread", "Index", "Create", new { Area = "Forum" }, null)</li>
                } 
            </ul>
            @using (Html.BeginForm("Index", "Search", new { Area = "Forum" }, FormMethod.Get, new { @class = "navbar-form navbar-right" }))
            {
                <div class="form-group">
                    @Html.TextBox("query", null, new { @class = "form-control", placeholder = "Search" })
                </div>
                    <input type="submit" class="btn btn-default" value="Search" />
            }
        </div>
    </div>
</nav>